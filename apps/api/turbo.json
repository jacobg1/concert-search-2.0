{
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "deploy": {
      "env": [
        "AWS_CONTAINER_CREDENTIALS_RELATIVE_URI",
        "SERVERLESS_ACCESS_KEY"
      ]
    },
    "deploy:test": {
      "env": [
        "AWS_CONTAINER_CREDENTIALS_RELATIVE_URI",
        "SERVERLESS_ACCESS_KEY"
      ]
    },
    "dev": {
      "env": [
        "NODE_ENV",
        "METADATA_URL",
        "ADVANCED_SEARCH_URL"
      ]
    }
  }
}
