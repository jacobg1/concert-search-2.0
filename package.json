{
  "name": "concert-search-2.0",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "deploy": "turbo run deploy --filter=concert-search-api",
    "deploy:test": "turbo run deploy:test --filter=concert-search-api",
    "destroy:test": "turbo run destroy:test --filter=concert-search-api",
    "format": "prettier --write \"**/*.{ts,tsx,json}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:api": "turbo run test --filter=concert-search-api",
    "test:web": "turbo run test --filter=concert-search-web",
    "test:watch": "turbo run test:watch"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "turbo": "^2.9.6",
    "typescript": "5.8.3"
  },
  "dependencies": {
    "postcss": "8.5.15",
    "next": "16.2.9"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "postcss": "$postcss"
  },
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
